class Deck {
  constructor(obj) {
    this.cards = [10];
    this.matchedCards = 2;
    this.selectedCards = 0;
    this.matches = 0;
  }

  shuffle() {
    for (for (var i = 0; i < cards.count; i++))
  }

  checkSelectedCards() {
    for (for (var i = 0; i < selectedCards.count; i++))
  }

  moveToMatched() {

  }
};
