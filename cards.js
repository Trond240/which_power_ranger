class Card {
  constructor(image, matchedInfo) {
    // this.cardId = Number(card.cardId)
    this.image = image;
    this.matchedInfo = matchedInfo;
    this.matched = false;
  }

  matched() {
    
  }
}
